package com.example.newworlds;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
/*
This class is not being used. However, in a future extension, we would use it to direct to a screen where
users can input business information into a database.
 */
public class AddBusiness extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_add_business);
    }
}
